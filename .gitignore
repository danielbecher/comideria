# === Pastas locais deste repo que não devem ser versionadas ===
# Dump/backup completo do WordPress baixado via FTP (wp-admin, wp-content, etc.)
/backup-wordpress/

# === Logs e arquivos temporários ===
*.log
error_log
*.sql
*.sql.gz
*.tar
*.tar.gz
*.zip

# === Node/Composer (se algum tema/plugin usar build tools) ===
node_modules/
vendor/
composer.lock
package-lock.json
npm-debug.log

# === Editor / SO ===
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# === Segredos ===
.env
.env.*
